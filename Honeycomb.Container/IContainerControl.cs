namespace Honeycomb.Container
{
    using System;

    public interface IContainerControl
    {
//        ICellAddress LaunchActor(Uri loadPath, string qualifiedTypeName, uint intitialThreadCount = 1);
//        void ScaleActor(ICellAddress cellAddress, uint threadCount);
    }
}